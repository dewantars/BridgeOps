apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  labels:
    app: bridgeops
    component: database
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app: bridgeops
    component: database
  type: ClusterIP
